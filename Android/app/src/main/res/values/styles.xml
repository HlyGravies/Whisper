<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="android:popupMenuStyle">@style/MenuStyle</item>
    </style>

    <!-- Style for the Menu -->
    <style name="MenuStyle" parent="Widget.AppCompat.PopupMenu">
        <item name="android:textColor">#FFFFFF</item> <!-- Text color -->
        <item name="android:popupBackground">#000000</item> <!-- Background color -->
    </style>
</resources>